export function App() {
  return (
    <main style={{ fontFamily: "system-ui", maxWidth: 720, margin: "40px auto" }}>
      <h1>SoundCloud MVP</h1>
      <p>Monorepo environment is ready.</p>
      <p>Use API via <code>/api</code> through Nginx.</p>
    </main>
  );
}
